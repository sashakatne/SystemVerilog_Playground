Run:        Non-overlapping clock generator (delay-chain + cross-NAND)
Start UTC:  2026-05-24T03-23-22Z
End   UTC:  2026-05-24T03-23-29Z
Host:       mo.ece.pdx.edu
Questa:     2021.3_1   (/pkgs/mentor/questa/2021.3_1/questasim/bin)
License:    1717@mentor-lic.cecs.pdx.edu
Remote dir: ~/claude-runs/2026-05-24T03-23-22Z_nonoverlappingclockgenerator-sim

Purpose:
  Verify the structural non-overlapping clock generator: two NAND-fed
  delay chains produce CK1 and CK2 that are guaranteed never to be high
  simultaneously, plus their complements CK1_b and CK2_b. The TB drives
  a 10 ns CK source, then over 1000 ns monitors three invariants:
    1. CK1 and CK2 never overlap (CK1 & CK2 never true)
    2. CK1 toggles
    3. CK2 toggles
    4. CK1/CK1_b are complementary; CK2/CK2_b are complementary

Commands executed (from this directory):
  vsim -c -do 'do run.do; quit -f' > transcript.txt

Files compiled (per run.do):
  vlog -source -lint novckgen_structural.sv
  vlog -source -lint novckgen_tb.sv

Results:
  Compile and elaboration: every vlog/vopt stage Errors: 0, Warnings: 0.
  Simulation:
    1000 ns of continuous monitoring (overlap detection on every CK1/CK2
    change, plus toggle detection every CK edge once start_checking
    asserts after the first 2 clock cycles). Zero error prints emitted.
    Final verdict: 'No errors -- passed testbench'
  Waveform artifacts:
    The farm run produced dump.vcd. make_artifacts.py parsed that VCD into
    waveform_samples.csv and waveforms.png. dump.vcd is treated as generated
    simulator output and is not kept in the repo.
  FSM artifacts:
    No FSM image is applicable. The RTL has no state register or state
    transition process; it is a structural gate-level clock-phase generator.

Caveats:
  - The TB also opens a dump.vcd file via $dumpfile/$dumpvars. The dump
    is local to the run directory and not committed.
  - Questa reports no matching coverage data in novckgen.ucdb. The clean
    pass/fail evidence is the self-checking simulation transcript.
  - The buffer primitives have no delay annotation, so the RTL waveform
    shows CK1 and CK2 as complementary phases with zero visible dead band.

Files in this directory:
  MANIFEST.txt
  design.md
  make_artifacts.py
  novckgen.png
  novckgen_structural.sv
  novckgen_tb.sv
  run.do
  transcript.txt
  waveform_samples.csv
  waveforms.png
